#Files and permissions
Linux uses permissions to control acces to files.

Types of permissions:
r - read
w - write
x - execute

Useful (Permission) commands:

chmod - change permissions Example
chmod (permissions(0-7,0-7,0-7)) (file)
chown - change file owner
chown (user) (file)

ls -la - show permissions
